# AoC

Setup `.env` with `session=` taken from your browser cookies for aoc.

Install with `pip install -e .`

*Note: should work with out `-e` but this is untested.*

Download requested day with `aoc YEAR DAY`
